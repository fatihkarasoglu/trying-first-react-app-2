import { Helmet } from "react-helmet"

export default function Contact() {

    return(
        <div>
            <Helmet>
                <title>İletişim</title>
            </Helmet>
            Contact page
        </div>
    )
}